/config/config.php
/public/uploads/*
!/public/uploads/.gitkeep
!/public/uploads/articles/
/public/uploads/articles/*
!/public/uploads/articles/.gitkeep
/vendor/
.env

# Visual Studio workspace state (local IDE cache, not part of the project)
/.vs/

# tmp/ is a scratch folder, not used by app code — keep only the .htaccess
/tmp/*
!/tmp/.htaccess
